package zmeika;

import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

public class TestTest {

    @Test
    public void window() {
        Assert.assertTrue(zmeika.Test.window());
    }
}